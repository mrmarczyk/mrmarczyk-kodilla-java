package com.kodilla.spring.calculator.anotherversion;

public class CalculatorV2 {

    public void calulatorV2 (double a, double b) {
        // Add.makeCalculation(a,b);
        // Sub.makeCalculation(a,b);
        // Mul.makeCalculation(a,b);
        // Div.makeCalculation(a,b);
    }
}
